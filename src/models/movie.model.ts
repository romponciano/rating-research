export class Movie {
    constructor(
        public id: string,
        public Title: string,
        public Poster: string
    ) {
        this.id = id;
        this.Title = Title;
        this.Poster = Poster;
    }
}
